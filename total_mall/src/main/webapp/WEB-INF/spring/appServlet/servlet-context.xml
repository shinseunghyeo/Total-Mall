<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<!-- 파일 업로드 관련 MultipartResolver 빈 추가 -->
	<beans:bean id="multipartResolver" 
	            class="org.springframework.web.multipart.support.StandardServletMultipartResolver">
	</beans:bean>
	
	<!-- Interceptor 관련 설정 추가 -->
	<interceptors>
		<interceptor>
			<mapping path="/**/*.do"/>
	
			<exclude-mapping path="/"/>
			<exclude-mapping path="/TotalMall.do"/>
			
	 
			<exclude-mapping path="/member/join.do"/>
			<exclude-mapping path="/member/checkId.do"/>
			<exclude-mapping path="/member/emailCheck.do"/>
			<exclude-mapping path="/member/buyerJoinProcess1.do"/>
			<exclude-mapping path="/member/buyerJoinProcess2.do"/>
			<exclude-mapping path="/member/sellerJoinProcess1.do"/>
			<exclude-mapping path="/member/sellerJoinProcess2.do"/>
			<exclude-mapping path="/member/sellerJoinProcess3.do"/>
			<exclude-mapping path="/member/findidpw.do"/>
			<exclude-mapping path="/member/findId.do"/>
			<exclude-mapping path="/member/findPw.do"/>
			<exclude-mapping path="/member/findPwProcess.do"/>
			<exclude-mapping path="/member/login.do"/>
			<exclude-mapping path="/member/loginProcess.do"/>
			
	 
			<exclude-mapping path="/product/list.do"/>
			<exclude-mapping path="/product/item.do"/>
			<exclude-mapping path="/product/search.do"/>
			<exclude-mapping path="/product/item.do"/>
			
			<exclude-mapping path="/nav/WinterPage.do"/>
			<exclude-mapping path="/nav/DronPage.do"/>
			<exclude-mapping path="/nav/TotalboomPage.do"/>
			<exclude-mapping path="/nav/DrowingPage.do"/>
			<exclude-mapping path="/nav/ThousandPage.do"/>
			<exclude-mapping path="/nav/Timemall.do"/>
			<exclude-mapping path="/nav/Attendance.do"/>
			<exclude-mapping path="/nav/EventPage.do"/>
	 
			<exclude-mapping path="/customercenter/QnA.do"/>
			<exclude-mapping path="/customercenter/notice.do"/>

			<beans:bean id="loginInterceptor" class="human.smart.totalMall.interceptor.LoginInterceptor"/>
		</interceptor>
	
	</interceptors>
	
	<!-- 컴포넌트 스캔을 이용한 빈 자동 등록. 정의된 패키지와 하위 패키지에 있는 자바클래스의
	     어노테이션을 확인해서 빈으로 등록시켜줌  -->
	<context:component-scan base-package="human.smart.totalMall" />
	<context:component-scan base-package="human.smart.service" />
	
	
</beans:beans>
