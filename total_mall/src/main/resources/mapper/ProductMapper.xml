<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="human.smart.totalMall.ProductMapper">
	
	<select id="getProducts" parameterType="string" resultType="productVO">
		
SELECT * FROM tb_product WHERE category LIKE #{category}
ORDER BY p_idx DESC
LIMIT 20 OFFSET 0;
	</select>
	
	<!-- 상품등록: insert -->
	<insert id="insert" parameterType="ProductVO">
				insert into tb_product (m_idx, category, product_name, product_code, brand, summary, detail,start_date,end_date,origin_file_name1,save_file_name1,origin_file_name2,save_file_name2,price,sales_unit,p_reg_date,p_last_modified_date)
					   values(#{m_idx},#{category}, #{product_name}, #{product_code}, #{brand}, #{summary}, #{detail},#{start_date},#{end_date},#{origin_file_name1},#{save_file_name1},#{origin_file_name2},#{save_file_name2},#{price},#{sales_unit},#{p_reg_date},#{p_last_modified_date})
	</insert>
</mapper>