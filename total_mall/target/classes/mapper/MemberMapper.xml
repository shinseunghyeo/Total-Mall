<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="human.smart.totalMall.MemberMapper">
 	<!-- 회원가입 -->
 	<insert id="join" parameterType="memberVO">
 		insert into tb_member (member_id, member_pw, member_name, handphone, email, address1, address2, address3)
			values(#{member_id},#{member_pw},#{member_name},#{handphone},#{email},#{address1},#{address2},#{address3})
 	</insert>
 
 
 </mapper>